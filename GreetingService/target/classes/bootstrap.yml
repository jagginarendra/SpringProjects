
spring:
  application:
    name: myGreetingService


eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value} 
  client:
    serviceUrl:
      defaultZone: http://localhost:4001/eureka/
server:
  port: 0
      
   
    
  
  


